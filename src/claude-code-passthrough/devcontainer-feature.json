{
	"id": "claude-code-passthrough",
	"version": "0.5.0",
	"name": "Claude Code (with host credential passthrough)",
	"description": "Installs Anthropic's Claude Code CLI and bind-mounts the host's ~/.claude/.credentials.json into the container so you don't have to re-authenticate.",
	"documentationURL": "https://github.com/timcane/devcontainer-features/tree/main/src/claude-code-passthrough",
	"licenseURL": "https://github.com/timcane/devcontainer-features/blob/main/LICENSE",
	"keywords": [
		"claude",
		"claude-code",
		"anthropic",
		"ai",
		"llm"
	],
	"options": {
		"claudeVersion": {
			"type": "string",
			"default": "latest",
			"description": "Version of @anthropic-ai/claude-code to install from npm (e.g. 'latest', '1.2.3')."
		},
		"passthroughHostAuth": {
			"type": "boolean",
			"default": true,
			"description": "If true, wire the host's Claude Code auth into the container at postCreate: symlink ~/.claude/.credentials.json (so token refreshes flow back to the host) and copy ~/.claude.json (account/onboarding state — copied, not linked, because Claude rewrites it constantly with container-local paths). Both halves are needed together; either alone leaves Claude broken. The bind mounts are always declared; this flag only controls whether link/copy runs."
		},
		"passthroughHostConfig": {
			"type": "boolean",
			"default": false,
			"description": "If true, copy a curated subset of the host's global ~/.claude config into the container at postCreate: CLAUDE.md, settings.json, keybindings.json, and the commands/, skills/, and agents/ directories. Copied (not linked), so container edits stay container-local and never mutate the host's real config. Runtime state (projects/, todos/, shell-snapshots/, statsig/) and the separately-handled credential files are deliberately excluded. The bind mount of ~/.claude is always declared; this flag only controls whether the copy runs."
		}
	},
	"dependsOn": {
		"ghcr.io/devcontainers/features/node:1": {
			"version": "lts"
		}
	},
	"mounts": [
		{
			"type": "bind",
			"source": "${localEnv:HOME}${localEnv:USERPROFILE}/.claude/.credentials.json",
			"target": "/opt/claude-code-passthrough/.credentials.json"
		},
		{
			"type": "bind",
			"source": "${localEnv:HOME}${localEnv:USERPROFILE}/.claude.json",
			"target": "/opt/claude-code-passthrough/.claude.json"
		},
		{
			"type": "bind",
			"source": "${localEnv:HOME}${localEnv:USERPROFILE}/.claude",
			"target": "/opt/claude-code-passthrough/host-claude-config"
		}
	],
	"postCreateCommand": "/opt/claude-code-passthrough/link-credentials.sh"
}
